{
  "name": "sistema-ofs-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "backup": "powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\\scripts\\backup\\New-OFSBackup.ps1",
    "backup:schedule": "powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\\scripts\\backup\\Register-OFSBackupTask.ps1 -Time 10:00",
    "backup:check": "powershell -NoProfile -ExecutionPolicy RemoteSigned -File .\\scripts\\backup\\Check-OFSBackupHealth.ps1",
    "postinstall": "npm --prefix backend install --include=dev",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "engines": {
    "node": ">=18"
  }
}
